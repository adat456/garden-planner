import { useEffect } from "react";

const BedPlantingGrid: React.FC = function() {
    useEffect(() => {
        
    }, []);

    return (
        <table>
            <tbody>

            </tbody>
        </table>
    );
};

export default BedPlantingGrid;